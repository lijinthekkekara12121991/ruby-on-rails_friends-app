<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Friends App', root_path, class: "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'About us', home_about_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Friends', friends_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Add a new friend', new_friend_path, class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              My profile
            </a>
            <ul class="dropdown-menu bg-light" aria-labelledby="navbarDropdownMenuLink">
              <li><%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: 'btn btn-secondary' %></li>
              <li><%= button_to 'Edit profile', edit_user_registration_path, method: :get, class: 'btn btn-secondary' %></li>
              <%# <li><%= button_to 'Delete my account', user_registration_path, method: :delete, class: "btn btn-danger" %>
              <li><button type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteUserModal">
                    Cancel my account
                  </button>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign in', new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<div class="modal fade" id="deleteUserModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Unhappy with the account?</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <h2>Are you not happy with our services, or do you want the app no more?</h2>
        <h4>We are sad to hear that!</h4>
      </div>
      <div class="modal-footer">
        <%= button_to "Cancel my account", user_registration_path, method: :delete, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>
